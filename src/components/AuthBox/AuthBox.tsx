import styles from "./AuthBox.module.css"
const Box = () => {
    return (
        <div className={`h-[474px] w-screen ${styles.background}`}>
        </div>
    )
  };
  
  export default Box;
  